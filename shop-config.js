/*
 * A product becomes purchasable only when its status is "live" and its
 * checkoutUrl is a real HTTPS checkout page. Sale master files must never be
 * stored in this public website repository.
 */
window.BBX_SHOP = {
    products: {
        "digital-art-pack": {
            status: "coming",
            name: "LIFE IS FUN (Blue) — Digital Edition",
            checkoutUrl: ""
        }
    }
};
